# DB設計書記載ルール

## テーブル一覧の記載ルール

業務的な機能コンテキストごとに、「系統」サブセクションでグループ化し、下記のカラムを持つ表形式でテーブル名の一覧を記載する。

| カラム名                | 必須 | 一意性                 | 役割                                                                                      |
| ----------------------- | ---- | ---------------------- | ----------------------------------------------------------------------------------------- |
| No                      | 必須 | 系統ごとに一意         | ドキュメント内の表示順序付けを目的とする通し番号。                                        |
| テーブル論理名 (リンク) | 必須 | ドキュメント全体で一意 | 個別テーブル定義書へのリンク。人間が読むためのインデックス。                              |
| テーブル物理名          | 必須 | ドキュメント全体で一意 | 物理テーブル名 (snake_case) 生成の起点。                                                  |
| 概要                    | 必須 | -                      | テーブルの目的を簡潔に説明。                                                              |
| テーブル作成順序        | 必須 | ドキュメント全体で一意 | デプロイ依存関係の絶対的な基準。親 (参照される) → 子 (参照する) の順序を厳格に定義する。 |

「テーブル作成順序」は、テーブル追加やメンテナンスの便宜のため、順序番号にトビを設けることを許可する。

## 個別テーブル定義書の記載ルール

### テーブル論理名、物理名

- **1.テーブル概要**セクションに、テーブル論理名、物理名、概要を記載する。記載内容はテーブル一覧の内容と一致させる。

### 項目定義

- **2.カラム定義**セクションに、項目定義を記載する。

#### 共通ルール

- テーブル論理名は、原則日本語で命名する。
- テーブル物理名は、原則英単語の複数形とし、snake_caseで命名する。(例: `users` `reservations`)
- 各テーブルの主キーは36桁UUIDのサロゲートキーする。DBの関数あるいはAPIのロジックで発番することを想定する。
- カラム論理名は、原則日本語で命名する。
- カラム物理名は、原則英単語の単数形とし、snake_caseで命名する。(例: `name` `email`)
- 非NULL制約を設定する項目には、`NOTNULL`列に「`NN`」を記載する。`NOTNULL`列と`DEFAULT`列の記載が矛盾してはならない。(例: `NOTNULL`が`NN`かつ`DEFAULT`が`NULL`はNG)
- ユニーク制約を設定する項目は、ユニーク制約を構成する項目ごとに、`UNIQUE`列に`UKn`(nは複数のユニーク制約を識別するための通し番号)を記載する。(例: メールアドレスと削除フラグで`UK1`、携帯電話番号で`UK2`など)
- 真偽値のみを設定する項目は、要件に合わせて物理名を`is_xxx`あるいは`has_xxx`とし、`NN`と`DEFAULT`の設定を必須とする。
  - 下記の監査項目の例では、MySQLを前提としているため`TINYINT`としている。RDBMSによって、`BIT`や`BOOLEAN`等、適切な型を指定する。
- 各テーブルには、以下に示す監査項目を必須とする。

| カラム論理名 | カラム物理名  | 型(桁,精度) | PK  | FK  | UNIQUE | NOTNULL | DEFAULT           | 備考                                       |
| ------------ | ------------- | ----------- | --- | --- | ------ | ------- | ----------------- | ------------------------------------------ |
| 登録日時     | registered_at | TIMESTAMP   | -   | -   | -      | NN      | CURRENT_TIMESTAMP | -                                          |
| 登録者       | registered_by | VARCHAR(36) | -   | -   | -      | NN      | -                 | -                                          |
| 更新日時     | updated_at    | TIMESTAMP   | -   | -   | -      | -       | NULL              | 登録時NULL、更新時は必須(ロジック側で保証) |
| 更新者       | updated_by    | VARCHAR(36) | -   | -   | -      | -       | NULL              | 登録時NULL、更新時は必須(ロジック側で保証) |
| 削除フラグ   | is_deleted    | TINYINT(1)  | -   | -   | -      | NN      | 0                 | -                                          |

※登録者、更新者は、画面からのリクエストであればユーザーのUUID、ロジック上ユーザーIDが入らない場合は画面やバッチの識別子、SE対応の場合は作業チケット番号といった、データの登録・更新を行った者を特定できる情報を設定する想定である。

#### 1:1あるいは1:Nの1側のテーブルの主キー設定

- テーブル独自の`id`を主キーとして持つ。
  - テーブル独自の`id`は、論理名`ID`、物理名`id`とする。
  - `PK`列に「`PK`」を記載する。
  - `UNIQUE`列に「`-`」を記載する。
  - `FK`列に「`-`」を記載する。

| カラム論理名 | カラム物理名 | 型          | PK  | FK  | UNIQUE | NOTNULL | DEFAULT | 備考 |
| ------------ | ------------ | ----------- | --- | --- | ------ | ------- | ------- | ---- |
| ID           | id           | VARCHAR(36) | PK  | -   | -      | NN      | UUID    | -    |

#### 1:NのN側のテーブルの主キー設定

- テーブル独自の`id`を主キーとして持つ。定義ルールは「1:1あるいは1:Nの1側のテーブルの主キー設定」と同じである。
- 外部キーとして、親テーブルの`id`を持つ。
  - 親テーブルIDの論理名は、`テーブル論理名ID`とする。(例: `ユーザーID`)
  - 親テーブルIDの物理名は、`テーブル物理名_id`とする。(例: `users_id`)
  - 親テーブルIDに「`FK`」を記載する。

例:

| カラム論理名 | カラム物理名 | 型          | PK  | FK  | UNIQUE | NOTNULL | DEFAULT | 備考             |
| ------------ | ------------ | ----------- | --- | --- | ------ | ------- | ------- | ---------------- |
| ID           | id           | VARCHAR(36) | PK  | -   | -      | NN      | UUID    | 注文明細の主キー |
| 注文ID       | orders_id    | VARCHAR(36) | -   | FK  | -      | NN      | -       |                  |

#### N:Mの中間テーブルの主キー設定

- テーブル独自の`id`を主キーとして持つ。定義ルールは「1:1あるいは1:Nの1側のテーブルの主キー設定」と同じである。
- 外部キーとして、自テーブルが紐づけているテーブルの`id`を持つ。定義ルールは「1:NのN側」の「親テーブルID」と同様である。
- 複合ユニーク制約を構成する全ての外部キー項目に対して`UNIQUE`列に「`UK1`」を記載する(中間テーブルとして最も重要な関心事項であることを示すため、1番を使う)。また、備考欄に、中間テーブルの紐づけ先であることを明記する。

例:

| カラム論理名 | カラム物理名 | 型          | PK  | FK  | UNIQUE | NOTNULL | DEFAULT | 備考                                            |
| ------------ | ------------ | ----------- | --- | --- | ------ | ------- | ------- | ----------------------------------------------- |
| ID           | id           | VARCHAR(36) | PK  | -   | -      | NN      | UUID    | -                                               |
| ユーザーID   | user_id      | VARCHAR(36) | -   | FK  | UK1    | NN      | -       | 複合UK1を構成                                   |
| 部署ID       | dept_id      | VARCHAR(36) | -   | FK  | UK1    | NN      | -       | 複合UK1を構成。紐づけ先が一意であることを保証。 |

### インデックス定義

- **3.インデックス定義**セクションに、インデックスの定義を記載する。
- インデックス名は接頭辞`idx_`を必要とし、システム全体で一意となるように設定する。
- 項目定義でユニーク制約を付与している項目と同一のインデックスを定義してはならない。
- 追加のインデックスを定義しない場合は、表のヘッダのみを残しておくこと。
- B-Treeインデックスは、ソート順の指定を必須とする。
- Hashインデックスは、DBエンジンがHashインデックスをサポートしていることを前提とする。
- Hashインデックスは、ソート順を記載してはならない(`-`とすること)。
- Hashインデックスはイコール検索のみを目的とする。あいまい検索(`LIKE`, `BETWEEN`)あるいは`ORDER BY`を前提としたインデックスはB-Treeで定義すること。
- 既にB-Treeインデックスが存在するカラムに対して、ハッシュインデックスを重複して定義してはならない。
- 外部キーとして定義したカラムに対して、RDBMSがインデックスを自動作成しない場合(MySQL以外のRDBMSに移植する場合等)、参照整合性のチェックとJOIN処理の効率のため、B-Treeインデックスを必須とする。
  - 複合インデックスの一部として含まれる場合も可とする。
  - MySQLは通常外部キーにインデックスを自動付与するため、DB移植時に考慮すること。

例:(インデックス定義が必要な場合)

| インデックス物理名         | カラム物理名       | UNIQUE | インデックスタイプ | ソート順  | 備考                                                                                             |
| -------------------------- | ------------------ | ------ | ------------------ | --------- | ------------------------------------------------------------------------------------------------ |
| idx_users_name             | name               | NO     | B-Tree             | ASC       | ユーザー名検索(一般的な検索)                                                                     |
| idx_users_available_email  | email, is_deleted  | YES    | B-Tree             | ASC       | 有効ユーザーのメールアドレス一意性をロジック的に担保するため定義。ログイン時の高速検索にも利用。 |
| idx_orders_status_date     | status, order_date | NO     | B-Tree             | ASC, DESC | 特定ステータス(例: 未発送)の注文を日付降順で一覧表示する検索に利用                               |
| idx_products_code_hash     | product_code       | YES    | Hash               | -         | 製品コードによる完全一致検索のみに利用                                                           |
| idx_order_details_order_id | order_id           | NO     | B-Tree             | ASC       | 外部キーによる親テーブルとのJOIN処理効率化(MySQLでは作成不要)                                    |

例:(インデックス定義が不要な場合)

| インデックス物理名 | カラム物理名 | UNIQUE | インデックスタイプ | ソート順 | 備考 |
| ------------------ | ------------ | ------ | ------------------ | -------- | ---- |

### 外部キー定義

- **4.外部キー定義**セクションに、外部キーの定義を記載する。
- 外部キーは接頭辞`fk_`を必要とし、システム全体で一意となるように設定する。
- 項目定義で`FK`の設定をした項目は、外部キー定義への記載を必須とする。
- `ON DELETE` 及び `ON UPDATE`項目は、要件に応じて`Cascade` `Restrict` `NoAction` `SetNull` `SetDefault`のいずれかを指定する。
  - 記載内容をPrismaの`onDelete` `onUpdate`定義に直接変換するため、CamelCaseとしている。
  - システム設定値の参照など、削除や主キーの変更が原則発生しない場合は`ON DELETE` `ON UPDATE`とも`NoAction`とする。
  - 注文明細テーブルから注文テーブルへの参照など、親テーブルの削除が要件上発生する場合は`ON DELETE`を`Cascade`、`ON UPDATE`を`NoAction`とする。
  - それ以外の設定を行う場合、備考欄に要件上の理由を明記すること。
  - RDBMSによっては`Cascade` `SetNull`の使用が厳しい(SQL Server等)場合があるので注意すること。
- 外部キーを定義しない場合は、表のヘッダのみを残しておくこと。

例:(外部キー定義が必要な場合)

| 外部キー物理名        | 参照元カラム物理名 | 参照先テーブル物理名 | 参照先カラム物理名 | ON DELETE | ON UPDATE | 備考                             |
| --------------------- | ------------------ | -------------------- | ------------------ | --------- | --------- | -------------------------------- |
| fk_reservations_users | users_id           | users                | id                 | Cascade   | NoAction  | ユーザーが削除されたら予約も削除 |

例:(外部キー定義が不要な場合)

| 外部キー物理名 | 参照元カラム物理名 | 参照先テーブル物理名 | 参照先カラム物理名 | ON DELETE | ON UPDATE | 備考 |
| -------------- | ------------------ | -------------------- | ------------------ | --------- | --------- | ---- |

## その他の記載ルール

- 単一責任原則に基づき、1つのテーブルに2つ以上の役割を持たせてはならない。
- 同じく、1つのカラムに2つ以上の役割を持たせてはならない。

(C)2025 Ryo ICHIKAWA
